# إعدادات Netlify
[build]
  publish = "."          # مجلد الموقع
  functions = "netlify/functions"  # مجلد الـ Functions

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"